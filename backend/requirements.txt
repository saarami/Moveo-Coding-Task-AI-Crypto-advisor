# Phase 2 — FastAPI backend setup
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Phase 3+ (database — not yet active)
# sqlalchemy==2.0.36
# alembic==1.14.0
# psycopg2-binary==2.9.10

# Phase 5+ (auth — not yet active)
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4
# python-multipart==0.0.12

# Phase 8+ (external APIs — not yet active)
# httpx==0.28.0
